name: Build and Test
on:
  push:
    branches:
      - master
jobs:
  Compile:
    runs-on: ubuntu-latest
    steps:
      - run: make build
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
